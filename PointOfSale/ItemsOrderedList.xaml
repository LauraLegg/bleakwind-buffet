<UserControl x:Class="PointOfSale.ItemsOrderedList"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:PointOfSale"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="300">
    <Grid>
        <!--Size of the grid is 1 column by 2 rows-->
        <Grid.ColumnDefinitions>
            <ColumnDefinition/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition/>
            <RowDefinition Height="8*"/>
        </Grid.RowDefinitions>
        
        <!--How the list of ordered items is going to be displayed-->
        <Border BorderThickness="2" BorderBrush="Gray">
            <TextBlock TextAlignment="Center" FontFamily="arial" FontWeight="Bold" FontSize="25" TextDecorations="underline">
                Items Ordered:
            </TextBlock>
        </Border>
        <Border Grid.Row="1">
            <ListView/>
        </Border>
    </Grid>
</UserControl> 
